Всё о баскетболе

<%= form_with url: root_path, data: {turbo: false}, method: :get do |form| %>
    <%= form.hidden_field :act, :value => 'basketball' %>
    <%= form.submit("Записаться", class: 'btn btn-primary')%>
<% end %>