<!DOCTYPE html>
<html>
  <head>
    <title>FitnesClub</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

     <!-- CSS only -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <header>
  <nav class="navbar navbar-light bg-light">
    <ul class = "nav col-12 col-lg-auto mb-2 mb-md-0">
      <% if true %>        
            <li><%= link_to 'Услуги', join_path, class: 'nav-link px-2' %></li>
            <li><%= link_to 'Тренеры', root_path, class: 'nav-link px-2' %></li>
      <% end %>
    </ul>

    <ul class = "nav col-12 col-lg-auto mb-2 mb-md-0">
      <% if false %>
          <span class="navbar-brand mb-0 h1"><%= current_user.name %></span>          
          <li><%= link_to 'Выйти', root_path, data: {turbo_method: :delete}, class: 'nav-link px-2', id: 'quit_button' %></li>
      <% else %>
          <li><%= link_to 'Регистрация', root_path, class: 'nav-link px-2' %></li>
          <li><%= link_to 'Вход', root_path, class: 'nav-link px-2' %></li>
      <% end %>
    </ul>
  </nav>
  </header>

  <body>
    <%= yield %>
  </body>

</html>
