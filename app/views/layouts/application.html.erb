<!DOCTYPE html>
<html>
  <head>
    <title>RomanOff</title>
    <link rel="icon" href="/assets/dumbbell.png" >
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <header>
  <nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid">
      <a class="navbar-brand">
          <%= link_to root_path do %>
            <%= image_tag "romanoff.jpg", size: "100x100", :alt => "Лого" %>
          <% end %>
      </a>
      <ul class = "navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item"><%= link_to 'О нас', root_path, class: 'nav-link px-2' %></li>        
              <li class="nav-item">
                <div class="dropdown">
                  <%= link_to 'Услуги', join_path, class: 'nav-link px-2' %>
                    <ul class="dropdown-menu">
                        <% Service.all.each do |i| %>
                          <%= link_to i.name, service_path(:service => i.name), class: 'nav-link px-2' %></li>  
                        <% end %>
                    </ul>
                </div>
              </li>
              <li class="nav-item"><%= link_to 'Тренеры', root_path, class: 'nav-link px-2' %></li>
      </ul>

      <ul class = "nav col-12 col-lg-auto mb-2 mb-md-0">
        <% if user_signed_in? %>
            <span class="navbar-brand mb-0 h1"><%= current_user.name %></span>
            <li><%= link_to 'Профиль', user_profile_path, class: 'nav-link px-2' %></li>        
            <li><%= link_to 'Выйти', destroy_user_session_path, data: {turbo_method: :delete}, class: 'nav-link px-2', id: 'quit_button' %></li>
        <% else %>
            <li><%= link_to 'Регистрация', new_user_registration_path, class: 'nav-link px-2' %></li>
            <li><%= link_to 'Вход', new_user_session_path, class: 'nav-link px-2' %></li>
        <% end %>
      </ul>
    </div>
  </nav>
  </header>

  <body>
    <% if alert %>
      <p class="alert alert-warning"><%= alert %></p>
    <% end %>

    <% if notice %>
    <p class="alert alert-success"><%= notice %></p>
    <% end %>
    <%= yield %>
  </body>

</html>
